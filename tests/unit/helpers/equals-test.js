
import { equals } from 'ember-ab-initio/helpers/equals';
import { module, test } from 'qunit';

module('Unit | Helper | equals');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = equals([42]);
  assert.ok(result);
});

